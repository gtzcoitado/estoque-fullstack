{
    "name": "estoque-fullstack",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "heroku-postbuild": "cd frontend/web-estoque && npm install && npm run build",
      "start": "cd backend && npm install && node server.js"
    }
  }
  